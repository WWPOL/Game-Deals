// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/WWPOL/Game-Deals/backend/ent/deal"
	"github.com/WWPOL/Game-Deals/backend/ent/image"
	"github.com/WWPOL/Game-Deals/backend/ent/user"
)

// DealCreate is the builder for creating a Deal entity.
type DealCreate struct {
	config
	mutation *DealMutation
	hooks    []Hook
}

// SetName sets the "name" field.
func (dc *DealCreate) SetName(s string) *DealCreate {
	dc.mutation.SetName(s)
	return dc
}

// SetExpiresAt sets the "expires_at" field.
func (dc *DealCreate) SetExpiresAt(t time.Time) *DealCreate {
	dc.mutation.SetExpiresAt(t)
	return dc
}

// SetPrice sets the "price" field.
func (dc *DealCreate) SetPrice(f float64) *DealCreate {
	dc.mutation.SetPrice(f)
	return dc
}

// SetLink sets the "link" field.
func (dc *DealCreate) SetLink(s string) *DealCreate {
	dc.mutation.SetLink(s)
	return dc
}

// AddImageIDs adds the "image" edge to the Image entity by IDs.
func (dc *DealCreate) AddImageIDs(ids ...int) *DealCreate {
	dc.mutation.AddImageIDs(ids...)
	return dc
}

// AddImage adds the "image" edges to the Image entity.
func (dc *DealCreate) AddImage(i ...*Image) *DealCreate {
	ids := make([]int, len(i))
	for j := range i {
		ids[j] = i[j].ID
	}
	return dc.AddImageIDs(ids...)
}

// AddCreatedByIDs adds the "created_by" edge to the User entity by IDs.
func (dc *DealCreate) AddCreatedByIDs(ids ...int) *DealCreate {
	dc.mutation.AddCreatedByIDs(ids...)
	return dc
}

// AddCreatedBy adds the "created_by" edges to the User entity.
func (dc *DealCreate) AddCreatedBy(u ...*User) *DealCreate {
	ids := make([]int, len(u))
	for i := range u {
		ids[i] = u[i].ID
	}
	return dc.AddCreatedByIDs(ids...)
}

// Mutation returns the DealMutation object of the builder.
func (dc *DealCreate) Mutation() *DealMutation {
	return dc.mutation
}

// Save creates the Deal in the database.
func (dc *DealCreate) Save(ctx context.Context) (*Deal, error) {
	return withHooks(ctx, dc.sqlSave, dc.mutation, dc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (dc *DealCreate) SaveX(ctx context.Context) *Deal {
	v, err := dc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (dc *DealCreate) Exec(ctx context.Context) error {
	_, err := dc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (dc *DealCreate) ExecX(ctx context.Context) {
	if err := dc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (dc *DealCreate) check() error {
	if _, ok := dc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Deal.name"`)}
	}
	if _, ok := dc.mutation.ExpiresAt(); !ok {
		return &ValidationError{Name: "expires_at", err: errors.New(`ent: missing required field "Deal.expires_at"`)}
	}
	if _, ok := dc.mutation.Price(); !ok {
		return &ValidationError{Name: "price", err: errors.New(`ent: missing required field "Deal.price"`)}
	}
	if _, ok := dc.mutation.Link(); !ok {
		return &ValidationError{Name: "link", err: errors.New(`ent: missing required field "Deal.link"`)}
	}
	return nil
}

func (dc *DealCreate) sqlSave(ctx context.Context) (*Deal, error) {
	if err := dc.check(); err != nil {
		return nil, err
	}
	_node, _spec := dc.createSpec()
	if err := sqlgraph.CreateNode(ctx, dc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	dc.mutation.id = &_node.ID
	dc.mutation.done = true
	return _node, nil
}

func (dc *DealCreate) createSpec() (*Deal, *sqlgraph.CreateSpec) {
	var (
		_node = &Deal{config: dc.config}
		_spec = sqlgraph.NewCreateSpec(deal.Table, sqlgraph.NewFieldSpec(deal.FieldID, field.TypeInt))
	)
	if value, ok := dc.mutation.Name(); ok {
		_spec.SetField(deal.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := dc.mutation.ExpiresAt(); ok {
		_spec.SetField(deal.FieldExpiresAt, field.TypeTime, value)
		_node.ExpiresAt = value
	}
	if value, ok := dc.mutation.Price(); ok {
		_spec.SetField(deal.FieldPrice, field.TypeFloat64, value)
		_node.Price = value
	}
	if value, ok := dc.mutation.Link(); ok {
		_spec.SetField(deal.FieldLink, field.TypeString, value)
		_node.Link = value
	}
	if nodes := dc.mutation.ImageIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   deal.ImageTable,
			Columns: []string{deal.ImageColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(image.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := dc.mutation.CreatedByIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   deal.CreatedByTable,
			Columns: []string{deal.CreatedByColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DealCreateBulk is the builder for creating many Deal entities in bulk.
type DealCreateBulk struct {
	config
	err      error
	builders []*DealCreate
}

// Save creates the Deal entities in the database.
func (dcb *DealCreateBulk) Save(ctx context.Context) ([]*Deal, error) {
	if dcb.err != nil {
		return nil, dcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(dcb.builders))
	nodes := make([]*Deal, len(dcb.builders))
	mutators := make([]Mutator, len(dcb.builders))
	for i := range dcb.builders {
		func(i int, root context.Context) {
			builder := dcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DealMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, dcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, dcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, dcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (dcb *DealCreateBulk) SaveX(ctx context.Context) []*Deal {
	v, err := dcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (dcb *DealCreateBulk) Exec(ctx context.Context) error {
	_, err := dcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (dcb *DealCreateBulk) ExecX(ctx context.Context) {
	if err := dcb.Exec(ctx); err != nil {
		panic(err)
	}
}
